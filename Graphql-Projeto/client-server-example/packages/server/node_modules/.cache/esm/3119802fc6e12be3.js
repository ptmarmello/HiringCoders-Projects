let gql;_c6e‍.x([["typeDefs",()=>typeDefs],["resolvers",()=>resolvers]]);_c6e‍.w("apollo-server-express",[["gql",["gql"],function(v){gql=v}]]);

       const typeDefs = gql` 
    type Demand{
        id: ID!
        name: String!
        client: Client!
        deadline: String
    }

    extends type Query{
        demands: [Demand]!
    }
`;


       const resolvers = {
    Query: {
        demands: () => [],
    },
};